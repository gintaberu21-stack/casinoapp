# CASINO DUEL — Claude Code 引き継ぎ

## 目的と現在地

専門学校の文化祭でスマートフォンから遊ぶ、カジノ風ブラックジャック。サインイン不要の公開サイト。現在は HTML/CSS/Vanilla JavaScript だけで動くフロントエンドで、バックエンドは未実装。今後もフロントエンドの修正要望が続く予定なので、未依頼の機能や仕様は決め打ちせず、その都度ユーザーの指示に従うこと。

公開URL: https://casino-duel-festival.kwgd422vrj.chatgpt.site/

## プロジェクト構成

- `dist/index.html`: ロビー、対戦画面、説明と各種オーバーレイ
- `dist/styles.css`: スマホ向け画面、タロット風必殺技カード、演出
- `dist/js/deck.js`: 通常カードと点数計算
- `dist/js/skills.js`: 重複なしの必殺技カード8種類
- `dist/js/game.js`: ゲーム状態、3ラウンド、CPU、精算
- `dist/js/ui.js`: 描画、カード選択、コイントス、結果とアニメーション
- `dist/js/app.js`: UIイベントとゲーム進行
- `dist/js/sound.js`: Web Audioで合成する効果音。`casino:cue`イベントを受け取って鳴らす
- `dist/js/match.js`: ID採番・相手検索・対戦申し込みの窓口。中身は暫定のローカル実装
- `dist/js/matchUI.js`: 相手検索ページの描画と操作、申し込みダイアログと対戦部屋
- `server/index.js`: Express + WebSocketのハブ。`dist`の配信、ID採番、在席一覧、申し込み・承認、対戦中の中継
- `server/db.js` / `server/store.js`: MongoDB接続と保存。`MONGODB_URI`が無いときはメモリ保存で動く
- `render.yaml`: Renderのweb service設定。`MONGODB_URI`は`sync: false`でダッシュボード入力
- `.openai/hosting.json`: 旧Sitesプロジェクトの設定。静的配信をRenderへ移すなら不要になる。

## 実装済みの主な仕様

- ひとり対CPU、または一台の端末を交代して使うふたり対戦。サインインなし。
- 持ちチップ500からの**ベット制**。1試合は5ラウンド（`game.js`の`MATCH_ROUNDS`）。ラウンド開始前に賭け額を自分で決め（`ui.requestBet`）、勝敗でその額をやり取りする。最後にチップが多い方の勝ち。
- チップが0になったらその時点で試合終了・敗北（`settle()`の`bankrupt`）。賭けられる上限は双方の残高の少ない方（`maxWager()`）で、相手が払えない額は賭けられない。最小単位は50。
- **ラウンドごとに勝敗画面は出さない**。代わりにお互いのチップだけを見せる`showStanding`を表示する。勝敗画面（`showResult`）は試合の最後だけで、勝敗は**ラウンドの勝ち数ではなく最終チップ**で決まる（`matchOutcome`）。文字はソロだと`You Win!` / `You lose`、ふたりで対決だと`PLAYER 1 WIN` / `PLAYER 2 WIN`。
- チップ表示は相手が右上のヘッダー（`dealer-chip-count`）、自分が自分の手札の左下（`player-chip-count`）。
- 毎ラウンドの先攻はコイントスで決定し、先攻・後攻を大きく表示。ターンが交代するのはHITかSTANDをしたときだけ（`app.js`の`passTurn()`）。
- 試合開始時に8種類から重複なしで必殺技を各3枚配布。5ラウンドを通して保持し、使ったカードは復活しない。REVERSEとSTEALは削除済み。
- 必殺技にはランクがある。A=金（SHUFFLE / SELECT REVERSE / TRIPLE BET）、B=従来の紫（DOUBLE BET / EXTRA DRAW / LOCK）、C=シルバー（FUTURE SIGHT / SHIELD）。色は`styles.css`の`.is-rank-a` / `.is-rank-c`で、カード右上にランクのバッジを出す。
- 配布は1枚引くごとにランクを重み付き抽選（`RANK_WEIGHTS` A20% / B40% / C40%）してから、そのランクの中から1枚選ぶ。プレイヤーとCPUで交互に引く。**山札が8枚しかないため、6枚配り終えた時点の実際の比率はA27% / B43% / C30%程度になる**（Cが2枚しかなく先に尽きるため）。厳密に20/40/40へ寄せるならCランクのカードを増やす必要がある。
- DOUBLE BET / TRIPLE BETは固定額ではなく、そのラウンドの賭け額を2倍・3倍にする（`multiplyWager`）。上限は`maxWager()`。
- 必殺技はターンを消費しない。使ったあとも同じ人の手番が続き、続けて別の必殺技やHIT / STANDを選べる。ただしCPUは演出が長くなりすぎるので1ターンに1枚まで（`cpuSpecialUsed`）。
- SHUFFLEは相手の全手札を公開して双方から1枚ずつ選んで交換。EXTRA DRAWは1枚引き、自分の手札から1枚捨てる。
- CPUも攻撃系必殺技を使用。必殺技名と効果を長めに表示し、交換・削除対象のカードを光らせて処理。
- CPUのテンポは`app.js`の`CPU_PACE`で調整。思考1.4秒 → HIT/STAND宣言のトースト0.9秒 → 行動後1.2秒。必殺技の演出はCPUのとき2.9秒、対象カードの点灯は1.6秒。
- 21は即勝利にしない。21以上になった人は自動でSTAND扱い（`game.refreshAutoStand()`）になるだけで、勝敗は双方のターンが終わってから決まる。必殺技でカードを減らされて21未満に戻ったら自動STANDは解除され、また行動できる。自分で押したSTANDは解除しない。双方が21なら他に打つ手がないのでその場で決着。
- 決着の演出は`finishRound`で`SHOWDOWN`表示0.8秒 → 伏せ札を公開して0.7秒 → チップ増減 → 1.3秒後に結果。最終ラウンドは結果に`is-final`が付き、LOBBYボタンが中央になる。
- 結果画面の演出（枠色・花吹雪・効果音・画面揺れ）は`showResult`の`mood`で決める。通常ラウンドはそのラウンドの勝敗、最終ラウンドは**試合全体の勝敗**。最終ラウンドだけ勝って試合に負けたときに花吹雪と勝利音が出ないようにするため。負け・引き分けでは花吹雪を出さない。
- 効果音は音源ファイルを持たずWeb Audioで合成。`ui.cue(name)`が`casino:cue`を飛ばし、`sound.js`が鳴らす。AudioContextは最初のタップで解錠する。
- ボタンの音は`app.js`のdocument全体のクリック監視でまとめて鳴らす（カチッ＝`press`）。別の音にしたいボタンは`data-cue`属性で指定する。カード配布は`deal`（シュッ）、コイントスは`coin`（弾く音＋1.1秒後の着地音でアニメに同期）、必殺技は`special`（上昇＋きらめき）。
- チップ変動では双方にプラス／マイナス数値を出し、残高を段階的に増減。カード交換・引き直し後は新しいカードの登場アニメーション。
- 最終ラウンドの結果を表示したあとメイン画面へ自動で戻る。

## ふたりで対決のマッチング（作りかけ）

- メイン画面を開いた時点でIDを採番する。最初の人が1番、次の人が2番。
- 「ふたりで対決」を押すと相手検索ページ（`match-screen`）へ。検索欄に相手のIDを入れると下に相手が出て、押すと相手に申し込みが飛ぶ。相手が承認すると双方が対戦部屋に入り、ホストがGAME STARTを押すと両方の画面で対戦が始まる。
- デザインは支給されたクラブ検索画面の構成（検索パネル／バナー／フィルタ付きリスト）をCasino Duelの配色に置き換えたもの。
- 通信は`dist/js/match.js`のWebSocketクライアントと`server/index.js`のハブ。ID採番・在席一覧・申し込み・承認・部屋・GAME STARTの合図まで**実際のサーバー経由で動作確認済み**。切断すると2秒ごとに自動再接続する。
- **未実装**: 対戦中の盤面同期。`sendState` / `sendAction`とサーバー側の中継（`state` / `action`）は用意してあるが、`app.js`がまだ使っていない。GAME START後は各端末が別々にゲームを進めるため、`matchUI.beginMatch()`でその旨をトースト表示している。方式は**ホスト端末が進行を担当**で決定済み。ゲスト用のスナップショットは自分が常に`player`席に見えるよう入れ替えて送る設計にすると、既存の描画をそのまま使える。
- ロビーの「ふたりで対決」＋GAME STARTによる従来の1台交代プレイはそのまま残してある。

## 作業時の注意

- 既存デザインとゲームの状態遷移を維持し、スマホ画面で表示と操作を確認する。
- `app.js`は演出中の`busy`管理と非同期処理に注意。チップの真の値は`game.js`で更新され、`ui.js`が表示を補間する。
- 通常カードの選択・交換はカードIDで行う。CPUと対人の両方で表示が崩れないことを確認する。
- `npm run check`と全JSの構文チェックを実行。静的サイトなのでビルドは不要。
- 公開中のサイトはSites。編集後の公開は既存プロジェクトのソースをプッシュし、同一コミットの静的`dist`と`.openai/hosting.json`を保存・デプロイする。公開範囲は現在publicで、変更しない。詳細な手順や認証は利用可能なSitesツール／スキルに従い、認証トークンをファイルへ書かない。

## 未確定事項

これからユーザーが追加のフロントエンド修正点を伝える予定。

端末をまたぐマッチングと対戦中の状態同期は、バックエンド（またはリアルタイム通信サービス）が決まらないと完成させられない。方式が決まるまで`match.js`のローカル実装のままにしておくこと。
